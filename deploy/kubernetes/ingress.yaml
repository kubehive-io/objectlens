apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: objectlens
  namespace: objectlens
spec:
  rules:
    - host: objectlens.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
