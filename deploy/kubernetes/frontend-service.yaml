apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: objectlens
spec:
  selector:
    app: objectlens
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
